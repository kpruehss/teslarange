import styled from 'styled-components';

const ControlDisplay = styled.p`
  font-family: 'Roboto';
  font-size: 25px;
  line-height: 25px;
  font-weight: 400;
  margin: 0;
  position: relative;
`;

export default ControlDisplay;